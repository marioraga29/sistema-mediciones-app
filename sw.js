// Este código permite que la app funcione offline una vez instalada
self.addEventListener('fetch', (event) => {
  // Aquí podrías cachear archivos, pero por ahora lo dejamos así para que sea instalable
});